services:
  auth-service:
    build:
      context: .
      dockerfile: ./apps/auth-service/Dockerfile
    working_dir: /app/apps/auth-service
    command: npm run dev
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=development
      - REDIS_HOST=redis
    volumes:
      - .:/app
      - /app/node_modules # Prevent local node_modules from overwriting inside container
    depends_on:
      - redis

  user-service:
    build:
      context: .
      dockerfile: ./apps/user-service/Dockerfile
    working_dir: /app/apps/user-service
    command: npm run dev
    ports:
      - "8081:8081"
    environment:
      - NODE_ENV=development
    volumes:
      - /app/node_modules # Prevent local node_modules from overwriting inside container
      - ./apps/user-service:/app/apps/user-service

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - auth-service
      - user-service

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    command: redis-server --bind 0.0.0.0 # Ensure Redis binds to all interfaces for Docker connectivity
